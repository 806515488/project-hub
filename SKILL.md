---
name: project-hub
description: 跨 AI 工具的项目维护架构。为新项目初始化「PROJECT.md 单一事实源 + 各 AI 工具入口文件」体系，或将旧项目散落的文档归并重整为该体系。当用户要求初始化项目维护架构、整理项目文档、建立 AGENTS.md/CLAUDE.md 体系、让多个 AI 工具协作维护同一项目时使用。
---

# project-hub

目标：让任何 AI 工具（Claude Code / Codex / Cursor / Gemini CLI / Copilot / Windsurf / Cline 等）进入项目时，用最小上下文开销获得准确的项目状态与规则。

## 核心规则（任何模式下不可违反）

1. **仓库内文档 > 工具本地记忆 > AI 自由发挥**。冲突时以仓库文档为准。
2. **PROJECT.md 是唯一事实源**，只做索引和总览，永久控制在 60 行以内。正文细节一律放它指向的子文档。
3. **入口文件只做指向**：每个工具的入口文件（CLAUDE.md、AGENTS.md 等）内容 3 行以内，指向 PROJECT.md，不承载任何正文。已存在的入口文件不覆盖，改写为指向。
4. **status.md 只留当前态**：只记录「当前进度、下一步、挂起问题」。旧状态不堆积，git 历史即历史。每次任务收尾必须更新它。
5. **落文档纪律**：文档写给「下次会话」看。只写结果（一行结论 + 文件路径 + 当前状态），不写过程叙述。坑只记「会再踩的」，一行一条，跟主题绑定的记入对应笔记，通用的记入 conventions.md 的坑区。
6. **索引防腐**：AI 修改 PROJECT.md 结构需用户确认；任何改动后验证索引指向的文件真实存在；单文件超过 300 行就拆分并更新索引。
7. **规则随产物走**：日常自维护规则（补入口文件、拆超长文档、修索引）必须内联进产物（PROJECT.md / conventions.md），保证无本 skill 的工具和会话也能照常执行。本 skill 只在初始化和改造时在场。
8. **单一权威源 + 版本标记**：规则格式在 skill 内只定义一处（入口格式权威源 = conventions 模板「入口文件标准」区，其他文件只引用）；修改 templates/ 下任何规则内容必须 bump 产物版本号（PROJECT.template.md 顶部注释，如 `<!-- project-hub v4 -->`），保证存量项目可识别、可同步。
9. **凭据不落文档**：任何产物文档（含样板示例、笔记、坑区）禁止出现真实凭据——密码、token、API key、连接串，一律写占位符（如 `<PASSWORD>`）；从现有代码挑样板、归并旧文档前先过滤凭据。真实值只存在于环境变量/配置中心。

## 任务检测（调用时逐项检查，可命中多项，依次执行，每项均需用户确认）

**第 1 项：架构是否存在**

- 无 `PROJECT.md` 且无散落文件 → **bootstrap**（新项目模式）→ 读 `references/bootstrap.md`
- 无 `PROJECT.md` 但有散落文件 → **retrofit**（旧项目模式）→ 读 `references/retrofit.md`

**第 2 项：已有架构则检查两笔欠账（都命中就都做）**

- **版本欠账**：PROJECT.md 顶部版本标记落后于 PROJECT.template.md 当前版本 → 只提示差异（新版改了什么、产物缺什么），确认后同步产物的规则区内容，不动其他文件；**同步后必须把产物的版本标记更新为当前版本**，否则永远处于「落后」状态反复提示
- **归并欠账**：存在未归位的散落文件——根目录的 .md（README / LICENSE / CHANGELOG 等标准根目录文件除外）、docs/ 根下不属于体系结构的文件（status.md / conventions.md / notes/ / attachments/ / 索引已引用的除外）→ 按 retrofit.md 的通用原则出归并方案表，确认后分批执行（破坏面评估照常适用，有外部引用的默认不动）

**两项都没有 → 无事可做，直接退出**。日常维护由产物内联规则自承载，日常会话不要调用本 skill。

## 收尾检查（每次执行完必做）

1. PROJECT.md 中索引指向的文件全部存在
2. 所有已安装 AI 工具的入口文件存在且内容是指向（不是正文）
3. 产物中无残留占位符（`<项目名>`、`<一句话>` 之类）
4. status.md 已更新为本次执行后的状态
5. 是否提交由用户决定，默认提醒用户 commit

## 维护本 skill 自身的规则

修改任何规则后：全文检查所有引用处的连带一致性（其他文件对被改内容的引用、相关流程是否仍成立）；涉及 templates/ 则按核心规则 8 bump 版本号。skill 总量保持克制——新问题优先由现有原则推导，推导不出才立新条目。
